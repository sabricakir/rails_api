<div class="max-w-7xl mx-auto mt-24">
  <div class="bg-white p-6 rounded-md shadow-md">
    <h1 class="text-2xl font-semibold mb-4">Reset your password</h1>

    <%= form_with(url: identity_password_reset_path, method: :patch) do |form| %>
      <% if @user.errors.any? %>
        <div class="mb-4">
          <h2 class="text-red-600"><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul class="list-disc ml-4">
            <% @user.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.hidden_field :sid, value: params[:sid] %>

      <div class="mb-4">
        <%= form.label :password, "New password", class: "block text-gray-700" %>
        <%= form.password_field :password, required: true, autofocus: true, autocomplete: "new-password", class: "w-full px-4 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300" %>
        <div class="text-sm text-gray-600 mt-1">12 characters minimum.</div>
      </div>

      <div class="mb-4">
        <%= form.label :password_confirmation, "Confirm new password", class: "block text-gray-700" %>
        <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password", class: "w-full px-4 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300" %>
      </div>

      <div class="mb-4">
        <%= form.submit "Save changes", class: "w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-md focus:outline-none focus:ring focus:border-blue-300" %>
      </div>
    <% end %>
  </div>
</div>
